<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    android:id="@+id/button_confirm"
    android:layout_gravity="center_horizontal"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageButton
        android:id="@+id/button_buy_book"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:src="@drawable/button_confirm"
        android:background="?android:windowBackground"
        app:layout_constraintStart_toStartOf="parent"
        android:scaleType="fitCenter"
        tools:ignore="MissingConstraints" />


    <TextView
        android:id="@+id/textView3"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/button_buy"
        android:textSize="14sp"
        android:textColor="?android:windowBackground"
        android:scaleType="fitCenter"
        android:fontFamily="@font/kantumruy_regular"
        app:layout_constraintStart_toStartOf="@id/button_buy_book"
        app:layout_constraintEnd_toEndOf="@id/button_buy_book"
        app:layout_constraintTop_toTopOf="@id/button_buy_book"
        app:layout_constraintBottom_toBottomOf="@id/button_buy_book"/>

</androidx.constraintlayout.widget.ConstraintLayout>